const postService = require("../services/postService");

exports.createPost= async (req, res) => {
            console.log(req.file);
    // try {
    //     const users = await postService.create();
    //     res.status(200).send(users);
        
    // } catch (err) {
    //     res.status(400).send(err);
    // }
    
}